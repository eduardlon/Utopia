---
import AppLayout from '../layouts/AppLayout.astro';
---

<AppLayout title="Mensajes">
  <div class="mm-app">
    <!-- Conversations List View -->
    <div class="mm-conversations-view" id="mm-conversations-view">
      <!-- Header -->
      <div class="mm-header">
        <h1>Mensajes</h1>
        <div class="mm-header-actions">
          <button class="mm-icon-btn" id="mm-search-btn" title="Buscar">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="11" cy="11" r="8"></circle>
              <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
            </svg>
          </button>
          <button class="mm-icon-btn" id="mm-new-chat-btn" title="Nueva conversación">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="12" y1="5" x2="12" y2="19"></line>
              <line x1="5" y1="12" x2="19" y2="12"></line>
            </svg>
          </button>
        </div>
      </div>

      <!-- Search Bar (hidden by default) -->
      <div class="mm-search-container" id="mm-search-container" hidden>
        <div class="mm-search-box">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="11" cy="11" r="8"></circle>
            <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
          </svg>
          <input type="text" placeholder="Buscar conversaciones..." id="mm-search-input" />
          <button class="mm-search-clear" id="mm-search-clear">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="18" y1="6" x2="6" y2="18"></line>
              <line x1="6" y1="6" x2="18" y2="18"></line>
            </svg>
          </button>
        </div>
      </div>

      <!-- Tabs -->
      <div class="mm-tabs">
        <button class="mm-tab active" data-tab="all">Todos</button>
        <button class="mm-tab" data-tab="unread">
          No leídos
          <span class="mm-tab-badge" id="mm-unread-badge" hidden>0</span>
        </button>
        <button class="mm-tab" data-tab="online">En línea</button>
      </div>

      <!-- Conversations List -->
      <div class="mm-list" id="mm-list">
        <!-- Loading Skeleton -->
        <div class="mm-skeleton" id="mm-skeleton">
          {[1, 2, 3, 4, 5, 6, 7, 8].map(() => (
            <div class="mm-skeleton-item">
              <div class="mm-skeleton-avatar"></div>
              <div class="mm-skeleton-content">
                <div class="mm-skeleton-row">
                  <div class="mm-skeleton-name"></div>
                  <div class="mm-skeleton-time"></div>
                </div>
                <div class="mm-skeleton-message"></div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Empty State -->
      <div class="mm-empty-state" id="mm-empty-state" hidden>
        <div class="mm-empty-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
          </svg>
        </div>
        <h3>Sin mensajes</h3>
        <p>No tienes conversaciones aún</p>
        <button class="mm-start-btn" id="mm-start-btn">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="12" y1="5" x2="12" y2="19"></line>
            <line x1="5" y1="12" x2="19" y2="12"></line>
          </svg>
          Iniciar chat
        </button>
      </div>
    </div>

    <!-- Chat View (hidden by default) -->
    <div class="mm-chat-view" id="mm-chat-view" hidden>
      <!-- Chat Header -->
      <div class="mm-chat-header">
        <button class="mm-back-btn" id="mm-back-btn">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M19 12H5M12 19l-7-7 7-7"></path>
          </svg>
        </button>
        <div class="mm-chat-avatar-wrap">
          <img src="/images/default-avatar.svg" alt="" class="mm-chat-avatar" id="mm-chat-avatar" />
          <span class="mm-chat-status-dot" id="mm-chat-status-dot"></span>
        </div>
        <div class="mm-chat-user-info">
          <span class="mm-chat-name" id="mm-chat-name">Usuario</span>
          <span class="mm-chat-status" id="mm-chat-status">En línea</span>
        </div>
        <a href="#" class="mm-chat-menu-btn" id="mm-chat-profile">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="1"></circle>
            <circle cx="19" cy="12" r="1"></circle>
            <circle cx="5" cy="12" r="1"></circle>
          </svg>
        </a>
      </div>

      <!-- Messages Container -->
      <div class="mm-messages-container" id="mm-messages-container">
        <div class="mm-messages-empty" id="mm-messages-empty">
          <div class="mm-messages-empty-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
            </svg>
          </div>
          <p>No hay mensajes</p>
          <span>¡Envía el primer mensaje!</span>
        </div>
      </div>

      <!-- Message Input -->
      <form class="mm-input-area" id="mm-message-form">
        <input type="text" placeholder="Escribe un mensaje..." id="mm-message-input" autocomplete="off" />
        <button type="submit" id="mm-send-btn" disabled>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="22" y1="2" x2="11" y2="13"></line>
            <polygon points="22 2 15 22 11 13 2 9 22 2"></polygon>
          </svg>
        </button>
      </form>
    </div>

    <!-- New Chat Modal -->
    <div class="mm-modal-overlay" id="mm-new-chat-modal" hidden>
      <div class="mm-modal-content glass">
        <div class="mm-modal-header">
          <h3>Nueva conversación</h3>
          <button class="mm-modal-close" id="mm-modal-close">✕</button>
        </div>
        <div class="mm-modal-search">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="11" cy="11" r="8"></circle>
            <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
          </svg>
          <input type="text" placeholder="Buscar amigo..." id="mm-modal-search-input" />
        </div>
        <div class="mm-modal-list" id="mm-modal-list">
          <div class="mm-modal-loading">
            <div class="mm-spinner"></div>
            <span>Cargando...</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</AppLayout>

<style>
  /* Main Container */
  .mm-app {
    min-height: calc(100vh - 64px);
    background: #0a0a0a;
    padding-bottom: 70px;
  }

  /* Conversations View */
  .mm-conversations-view {
    display: flex;
    flex-direction: column;
    height: calc(100vh - 64px);
  }

  /* Header */
  .mm-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 1.25rem;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    position: sticky;
    top: 0;
    z-index: 10;
  }

  .mm-header h1 {
    font-size: 1.5rem;
    font-weight: 700;
    color: white;
    margin: 0;
  }

  .mm-header-actions {
    display: flex;
    gap: 0.5rem;
  }

  .mm-icon-btn {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 50%;
    color: rgba(255, 255, 255, 0.7);
    cursor: pointer;
    transition: all 0.2s;
  }

  .mm-icon-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    color: white;
  }

  .mm-icon-btn svg {
    width: 20px;
    height: 20px;
  }

  /* Search Container */
  .mm-search-container {
    padding: 0.75rem 1rem;
    background: rgba(0, 0, 0, 0.4);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    animation: mmSlideDown 0.2s ease;
  }

  @keyframes mmSlideDown {
    from { opacity: 0; transform: translateY(-10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .mm-search-box {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1.5rem;
  }

  .mm-search-box svg {
    width: 18px;
    height: 18px;
    color: rgba(255, 255, 255, 0.4);
    flex-shrink: 0;
  }

  .mm-search-box input {
    flex: 1;
    background: none;
    border: none;
    color: white;
    font-size: 0.95rem;
    outline: none;
  }

  .mm-search-box input::placeholder {
    color: rgba(255, 255, 255, 0.4);
  }

  .mm-search-clear {
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.1);
    border: none;
    border-radius: 50%;
    color: rgba(255, 255, 255, 0.6);
    cursor: pointer;
  }

  .mm-search-clear svg {
    width: 14px;
    height: 14px;
  }

  /* Tabs */
  .mm-tabs {
    display: flex;
    gap: 0.25rem;
    padding: 0.5rem 1rem;
    background: rgba(0, 0, 0, 0.3);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .mm-tab {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.5rem 1rem;
    background: transparent;
    border: none;
    border-radius: 1.5rem;
    color: rgba(255, 255, 255, 0.5);
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
  }

  .mm-tab:hover {
    color: rgba(255, 255, 255, 0.7);
  }

  .mm-tab.active {
    background: rgba(34, 197, 94, 0.15);
    color: #22c55e;
  }

  .mm-tab-badge {
    background: #22c55e;
    color: white;
    font-size: 0.7rem;
    font-weight: 600;
    padding: 0.125rem 0.5rem;
    border-radius: 1rem;
    min-width: 18px;
    text-align: center;
  }

  /* Conversations List */
  .mm-list {
    flex: 1;
    overflow-y: auto;
  }

  .mm-item {
    display: flex;
    align-items: center;
    gap: 0.875rem;
    padding: 0.875rem 1.25rem;
    cursor: pointer;
    transition: background 0.15s;
    border-bottom: 1px solid rgba(255, 255, 255, 0.03);
  }

  .mm-item:hover {
    background: rgba(255, 255, 255, 0.03);
  }

  .mm-item:active {
    background: rgba(255, 255, 255, 0.05);
  }

  .mm-avatar-wrap {
    position: relative;
    flex-shrink: 0;
  }

  .mm-avatar {
    width: 52px;
    height: 52px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid rgba(34, 197, 94, 0.15);
  }

  .mm-status-dot {
    position: absolute;
    bottom: 2px;
    right: 2px;
    width: 14px;
    height: 14px;
    border-radius: 50%;
    border: 3px solid #0a0a0a;
  }

  .mm-status-dot.online {
    background: #22c55e;
    box-shadow: 0 0 8px rgba(34, 197, 94, 0.5);
  }

  .mm-status-dot.away {
    background: #f59e0b;
  }

  .mm-status-dot.offline {
    background: rgba(255, 255, 255, 0.3);
  }

  .mm-content {
    flex: 1;
    min-width: 0;
  }

  .mm-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.25rem;
  }

  .mm-name {
    font-size: 1rem;
    font-weight: 600;
    color: white;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .mm-time {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.4);
    flex-shrink: 0;
    margin-left: 0.5rem;
  }

  .mm-preview {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .mm-last-msg {
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.5);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
  }

  .mm-unread {
    background: #22c55e;
    color: white;
    font-size: 0.7rem;
    font-weight: 600;
    min-width: 20px;
    height: 20px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 6px;
    flex-shrink: 0;
  }

  .mm-check {
    width: 16px;
    height: 16px;
    color: #22c55e;
    flex-shrink: 0;
  }

  /* Empty State */
  .mm-empty-state {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 3rem 2rem;
    text-align: center;
  }

  .mm-empty-icon {
    margin-bottom: 1.5rem;
    color: rgba(34, 197, 94, 0.3);
  }

  .mm-empty-icon svg {
    width: 64px;
    height: 64px;
  }

  .mm-empty-state h3 {
    font-size: 1.25rem;
    font-weight: 600;
    color: white;
    margin: 0 0 0.5rem 0;
  }

  .mm-empty-state p {
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.5);
    margin: 0 0 1.5rem 0;
  }

  .mm-start-btn {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.5rem;
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
    border: none;
    border-radius: 2rem;
    color: white;
    font-size: 0.95rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }

  .mm-start-btn:hover {
    transform: scale(1.02);
    box-shadow: 0 4px 15px rgba(34, 197, 94, 0.3);
  }

  .mm-start-btn svg {
    width: 18px;
    height: 18px;
  }

  /* Skeleton */
  .mm-skeleton {
    display: flex;
    flex-direction: column;
  }

  .mm-skeleton-item {
    display: flex;
    align-items: center;
    gap: 0.875rem;
    padding: 0.875rem 1.25rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.03);
  }

  .mm-skeleton-avatar {
    width: 52px;
    height: 52px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.05);
    animation: mmShimmer 1.5s infinite;
  }

  .mm-skeleton-content {
    flex: 1;
  }

  .mm-skeleton-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 0.5rem;
  }

  .mm-skeleton-name {
    height: 14px;
    width: 120px;
    border-radius: 7px;
    background: rgba(255, 255, 255, 0.05);
    animation: mmShimmer 1.5s infinite;
  }

  .mm-skeleton-time {
    height: 10px;
    width: 40px;
    border-radius: 5px;
    background: rgba(255, 255, 255, 0.05);
    animation: mmShimmer 1.5s infinite 0.1s;
  }

  .mm-skeleton-message {
    height: 12px;
    width: 180px;
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.05);
    animation: mmShimmer 1.5s infinite 0.2s;
  }

  @keyframes mmShimmer {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 0.6; }
  }

  /* Chat View */
  .mm-chat-view {
    display: flex;
    flex-direction: column;
    height: calc(100vh - 64px);
    background: linear-gradient(180deg, #0a0a0a 0%, #0f0f0f 100%);
    animation: mmSlideIn 0.25s ease;
  }

  @keyframes mmSlideIn {
    from { transform: translateX(100%); }
    to { transform: translateX(0); }
  }

  .mm-chat-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.875rem 1rem;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }

  .mm-back-btn {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border: none;
    border-radius: 50%;
    color: rgba(255, 255, 255, 0.7);
    cursor: pointer;
    transition: all 0.2s;
  }

  .mm-back-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    color: white;
  }

  .mm-back-btn svg {
    width: 20px;
    height: 20px;
  }

  .mm-chat-avatar-wrap {
    position: relative;
    flex-shrink: 0;
  }

  .mm-chat-avatar {
    width: 42px;
    height: 42px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid rgba(34, 197, 94, 0.25);
  }

  .mm-chat-status-dot {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    border: 2px solid #0a0a0a;
  }

  .mm-chat-user-info {
    flex: 1;
    min-width: 0;
  }

  .mm-chat-name {
    display: block;
    font-size: 1rem;
    font-weight: 600;
    color: white;
    margin-bottom: 0.125rem;
  }

  .mm-chat-status {
    font-size: 0.75rem;
    color: #22c55e;
  }

  .mm-chat-menu-btn {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 50%;
    color: rgba(255, 255, 255, 0.7);
  }

  .mm-chat-menu-btn svg {
    width: 18px;
    height: 18px;
  }

  /* Messages Container */
  .mm-messages-container {
    flex: 1;
    overflow-y: auto;
    padding: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }

  .mm-messages-empty {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: rgba(255, 255, 255, 0.4);
  }

  .mm-messages-empty-icon {
    margin-bottom: 1rem;
    color: rgba(34, 197, 94, 0.3);
  }

  .mm-messages-empty-icon svg {
    width: 48px;
    height: 48px;
  }

  .mm-messages-empty p {
    margin: 0 0 0.25rem 0;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.6);
  }

  .mm-messages-empty span {
    font-size: 0.875rem;
  }

  /* Date Separator */
  .mm-date-sep {
    text-align: center;
    margin: 0.75rem 0;
  }

  .mm-date-sep span {
    font-size: 0.7rem;
    color: rgba(255, 255, 255, 0.35);
    background: rgba(255, 255, 255, 0.05);
    padding: 0.25rem 0.75rem;
    border-radius: 1rem;
  }

  /* Message */
  .mm-msg {
    display: flex;
    gap: 0.5rem;
    max-width: 85%;
    animation: mmMsgIn 0.2s ease;
  }

  @keyframes mmMsgIn {
    from { opacity: 0; transform: translateY(8px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .mm-msg.sent {
    align-self: flex-end;
    flex-direction: row-reverse;
  }

  .mm-msg.received {
    align-self: flex-start;
  }

  .mm-msg-avatar {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    object-fit: cover;
    border: 1.5px solid rgba(34, 197, 94, 0.2);
    flex-shrink: 0;
    align-self: flex-end;
  }

  .mm-msg.sent .mm-msg-avatar {
    display: none;
  }

  .mm-msg-body {
    display: flex;
    flex-direction: column;
    min-width: 0;
  }

  .mm-msg-sender {
    font-size: 0.7rem;
    color: #22c55e;
    font-weight: 500;
    margin-bottom: 0.2rem;
    padding-left: 0.25rem;
  }

  .mm-msg.sent .mm-msg-sender {
    display: none;
  }

  .mm-msg-bubble {
    padding: 0.75rem 1rem;
    border-radius: 1.25rem;
    max-width: 100%;
    word-wrap: break-word;
  }

  .mm-msg.sent .mm-msg-bubble {
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
    color: white;
    border-bottom-right-radius: 0.25rem;
  }

  .mm-msg.received .mm-msg-bubble {
    background: rgba(255, 255, 255, 0.08);
    color: white;
    border-bottom-left-radius: 0.25rem;
  }

  .mm-msg-bubble p {
    margin: 0;
    font-size: 0.95rem;
    line-height: 1.45;
  }

  .mm-msg-footer {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 0.25rem;
    margin-top: 0.25rem;
  }

  .mm-msg.received .mm-msg-footer {
    justify-content: flex-start;
  }

  .mm-msg-time {
    font-size: 0.65rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .mm-msg-check {
    width: 14px;
    height: 14px;
    color: rgba(255, 255, 255, 0.6);
  }

  .mm-msg-check.read {
    color: #22c55e;
  }

  /* Message Input */
  .mm-input-area {
    display: flex;
    gap: 0.75rem;
    padding: 0.875rem 1rem;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-top: 1px solid rgba(255, 255, 255, 0.05);
  }

  .mm-input-area input {
    flex: 1;
    padding: 0.875rem 1rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1.5rem;
    color: white;
    font-size: 1rem;
    outline: none;
    transition: border-color 0.2s;
  }

  .mm-input-area input::placeholder {
    color: rgba(255, 255, 255, 0.4);
  }

  .mm-input-area input:focus {
    border-color: rgba(34, 197, 94, 0.3);
  }

  .mm-input-area button {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
    border: none;
    border-radius: 50%;
    color: white;
    cursor: pointer;
    transition: all 0.2s;
    flex-shrink: 0;
  }

  .mm-input-area button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .mm-input-area button:not(:disabled):hover {
    transform: scale(1.05);
  }

  .mm-input-area button svg {
    width: 20px;
    height: 20px;
  }

  /* Modal */
  .mm-modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(8px);
    display: flex;
    align-items: flex-end;
    justify-content: center;
    z-index: 1000;
  }

  .mm-modal-content {
    width: 100%;
    max-width: 500px;
    max-height: 85vh;
    display: flex;
    flex-direction: column;
    border-radius: 1.5rem 1.5rem 0 0;
    padding: 1.25rem;
    background: rgba(19, 25, 23, 0.98);
    border: 1px solid rgba(34, 197, 94, 0.15);
    animation: mmModalUp 0.3s ease;
  }

  @keyframes mmModalUp {
    from { transform: translateY(100%); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
  }

  .mm-modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .mm-modal-header h3 {
    font-size: 1.125rem;
    font-weight: 600;
    color: white;
    margin: 0;
  }

  .mm-modal-close {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border: none;
    border-radius: 50%;
    color: rgba(255, 255, 255, 0.6);
    font-size: 1rem;
    cursor: pointer;
  }

  .mm-modal-search {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 1rem;
    margin-bottom: 1rem;
  }

  .mm-modal-search svg {
    width: 18px;
    height: 18px;
    color: rgba(255, 255, 255, 0.4);
  }

  .mm-modal-search input {
    flex: 1;
    background: none;
    border: none;
    color: white;
    font-size: 0.95rem;
    outline: none;
  }

  .mm-modal-search input::placeholder {
    color: rgba(255, 255, 255, 0.4);
  }

  .mm-modal-list {
    flex: 1;
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .mm-modal-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem;
    border-radius: 0.75rem;
    cursor: pointer;
    transition: background 0.15s;
  }

  .mm-modal-item:hover {
    background: rgba(34, 197, 94, 0.1);
  }

  .mm-modal-item img {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid rgba(34, 197, 94, 0.15);
  }

  .mm-modal-item-info {
    flex: 1;
  }

  .mm-modal-item-name {
    font-size: 0.95rem;
    font-weight: 500;
    color: white;
  }

  .mm-modal-item-username {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .mm-modal-loading {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 2rem;
    color: rgba(255, 255, 255, 0.5);
  }

  .mm-spinner {
    width: 16px;
    height: 16px;
    border: 2px solid rgba(34, 197, 94, 0.2);
    border-top-color: #22c55e;
    border-radius: 50%;
    animation: mmSpin 0.7s linear infinite;
  }

  @keyframes mmSpin {
    to { transform: rotate(360deg); }
  }

  /* Scrollbar */
  .mm-list::-webkit-scrollbar,
  .mm-messages-container::-webkit-scrollbar,
  .mm-modal-list::-webkit-scrollbar {
    width: 4px;
  }

  .mm-list::-webkit-scrollbar-thumb,
  .mm-messages-container::-webkit-scrollbar-thumb,
  .mm-modal-list::-webkit-scrollbar-thumb {
    background: rgba(34, 197, 94, 0.2);
    border-radius: 2px;
  }

  /* Desktop redirect message */
  .mm-desktop-redirect {
    display: none;
    text-align: center;
    padding: 2rem;
    color: rgba(255, 255, 255, 0.6);
  }

  @media (min-width: 768px) {
    .mm-desktop-redirect {
      display: block;
    }
    .mm-app > *:not(.mm-desktop-redirect) {
      display: none;
    }
  }
</style>

<script>
  import { supabase } from '../lib/supabase/client';

  // Redirect desktop users to the desktop version
  if (typeof window !== 'undefined') {
    const isMobile = window.innerWidth < 768;
    if (!isMobile) {
      window.location.href = '/messages';
    }
  }

  interface Friend {
    user_id: string;
    username: string;
    display_name: string | null;
    avatar_url: string | null;
    status: string;
    last_seen: string;
  }

  interface Message {
    id: string;
    sender_id: string;
    content: string;
    created_at: string;
  }

  interface Conversation {
    id: string;
    other_user: Friend;
    last_message: string;
    last_message_at: string;
    unread_count: number;
  }

  let friends: Friend[] = [];
  let conversations: Conversation[] = [];
  let filteredConversations: Conversation[] = [];
  let currentTab = 'all';
  let currentChat: { id: string; name: string; avatar: string; status: string } | null = null;
  let conversationId: string | null = null;
  let userId: string | null = null;
  let messages: Message[] = [];
  let realtimeChannel: any = null;

  // Elements
  const conversationsView = document.getElementById('mm-conversations-view')!;
  const chatView = document.getElementById('mm-chat-view')!;
  const convList = document.getElementById('mm-list')!;
  const convSkeleton = document.getElementById('mm-skeleton')!;
  const emptyState = document.getElementById('mm-empty-state')!;
  const searchBtn = document.getElementById('mm-search-btn')!;
  const searchContainer = document.getElementById('mm-search-container')!;
  const searchInput = document.getElementById('mm-search-input') as HTMLInputElement;
  const searchClear = document.getElementById('mm-search-clear')!;
  const newChatBtn = document.getElementById('mm-new-chat-btn')!;
  const startBtn = document.getElementById('mm-start-btn')!;
  const tabs = document.querySelectorAll('.mm-tab');
  const unreadBadge = document.getElementById('mm-unread-badge')!;

  const backBtn = document.getElementById('mm-back-btn')!;
  const chatAvatar = document.getElementById('mm-chat-avatar') as HTMLImageElement;
  const chatStatusDot = document.getElementById('mm-chat-status-dot')!;
  const chatName = document.getElementById('mm-chat-name')!;
  const chatStatus = document.getElementById('mm-chat-status')!;
  const chatProfile = document.getElementById('mm-chat-profile') as HTMLAnchorElement;
  const messagesContainer = document.getElementById('mm-messages-container')!;
  const messageForm = document.getElementById('mm-message-form') as HTMLFormElement;
  const messageInput = document.getElementById('mm-message-input') as HTMLInputElement;
  const sendBtn = document.getElementById('mm-send-btn') as HTMLButtonElement;

  const newChatModal = document.getElementById('mm-new-chat-modal')!;
  const modalClose = document.getElementById('mm-modal-close')!;
  const modalSearchInput = document.getElementById('mm-modal-search-input') as HTMLInputElement;
  const modalList = document.getElementById('mm-modal-list')!;

  // Initialize
  init();

  async function init() {
    const { data: { user } } = await supabase.auth.getUser();
    if (!user) {
      window.location.href = '/login';
      return;
    }
    userId = user.id;
    await loadFriends();
    await loadConversations();
    setupEventListeners();
  }

  async function loadFriends() {
    const { data: friendships } = await (supabase as any)
      .from('friendships')
      .select('requester_id, addressee_id')
      .eq('status', 'accepted')
      .or(`requester_id.eq.${userId},addressee_id.eq.${userId}`);

    if (!friendships || friendships.length === 0) return;

    const friendIds = friendships.map((f: any) =>
      f.requester_id === userId ? f.addressee_id : f.requester_id
    );

    const { data: profiles } = await (supabase as any)
      .from('profiles')
      .select('id, username, display_name, avatar_url')
      .in('id', friendIds);

    if (!profiles) return;

    const fiveMinutesAgo = new Date(Date.now() - 5 * 60 * 1000).toISOString();
    const { data: presence } = await (supabase as any)
      .from('user_presence')
      .select('user_id, status, last_seen')
      .gte('last_seen', fiveMinutesAgo)
      .in('user_id', friendIds);

    const presenceMap = new Map((presence || []).map((p: any) => [p.user_id, p]));

    friends = profiles.map((p: any) => {
      const pres = presenceMap.get(p.id);
      return {
        user_id: p.id,
        username: p.username,
        display_name: p.display_name,
        avatar_url: p.avatar_url,
        status: pres?.status || 'offline',
        last_seen: pres?.last_seen || '',
      };
    });
  }

  async function loadConversations() {
    try {
      const { data: participations } = await (supabase as any)
        .from('conversation_participants')
        .select('conversation_id')
        .eq('user_id', userId);

      convSkeleton.hidden = true;

      if (!participations || participations.length === 0) {
        emptyState.hidden = false;
        return;
      }

      const convIds = participations.map((p: any) => p.conversation_id);
      conversations = [];

      for (const convId of convIds) {
        const { data: otherParts } = await (supabase as any)
          .from('conversation_participants')
          .select('user_id')
          .eq('conversation_id', convId)
          .neq('user_id', userId);

        if (!otherParts || otherParts.length === 0) continue;

        const otherUserId = otherParts[0].user_id;

        const { data: profile } = await (supabase as any)
          .from('profiles')
          .select('id, username, display_name, avatar_url')
          .eq('id', otherUserId)
          .single();

        if (!profile) continue;

        const { data: lastMsg } = await (supabase as any)
          .from('messages')
          .select('content, created_at, sender_id')
          .eq('conversation_id', convId)
          .order('created_at', { ascending: false })
          .limit(1)
          .single();

        const friend = friends.find(f => f.user_id === otherUserId);

        conversations.push({
          id: convId,
          other_user: {
            user_id: profile.id,
            username: profile.username,
            display_name: profile.display_name,
            avatar_url: profile.avatar_url,
            status: friend?.status || 'offline',
            last_seen: friend?.last_seen || '',
          },
          last_message: lastMsg?.content || 'Sin mensajes',
          last_message_at: lastMsg?.created_at || '',
          unread_count: 0,
        });
      }

      conversations.sort((a, b) => {
        if (!a.last_message_at) return 1;
        if (!b.last_message_at) return -1;
        return new Date(b.last_message_at).getTime() - new Date(a.last_message_at).getTime();
      });

      filterAndRender();
    } catch (e) {
      console.error('Error loading conversations:', e);
      convSkeleton.hidden = true;
      emptyState.hidden = false;
    }
  }

  function filterAndRender() {
    const search = searchInput.value.toLowerCase();

    filteredConversations = conversations.filter(conv => {
      const name = conv.other_user.display_name || conv.other_user.username;
      const matchesSearch = name.toLowerCase().includes(search);

      if (currentTab === 'unread') {
        return matchesSearch && conv.unread_count > 0;
      } else if (currentTab === 'online') {
        return matchesSearch && (conv.other_user.status === 'online' || conv.other_user.status === 'away');
      }
      return matchesSearch;
    });

    renderConversations();
  }

  function renderConversations() {
    if (filteredConversations.length === 0) {
      if (conversations.length === 0) {
        emptyState.hidden = false;
        convList.innerHTML = '';
      } else {
        convList.innerHTML = `
          <div class="mm-empty-state">
            <p style="color: rgba(255,255,255,0.5);">No hay conversaciones en esta categoría</p>
          </div>
        `;
        emptyState.hidden = true;
      }
      return;
    }

    emptyState.hidden = true;
    convList.innerHTML = filteredConversations.map(conv => {
      const statusClass = conv.other_user.status === 'online' ? 'online' : conv.other_user.status === 'away' ? 'away' : 'offline';
      const time = formatTime(conv.last_message_at);
      const lastMsg = conv.last_message.substring(0, 45) + (conv.last_message.length > 45 ? '...' : '');

      return `
        <div class="mm-item" data-conv-id="${conv.id}" data-user-id="${conv.other_user.user_id}" data-name="${conv.other_user.display_name || conv.other_user.username}" data-avatar="${conv.other_user.avatar_url || '/images/default-avatar.svg'}" data-status="${conv.other_user.status}">
          <div class="mm-avatar-wrap">
            <img src="${conv.other_user.avatar_url || '/images/default-avatar.svg'}" alt="" class="mm-avatar" onerror="this.src='/images/default-avatar.svg'" />
            <span class="mm-status-dot ${statusClass}"></span>
          </div>
          <div class="mm-content">
            <div class="mm-row">
              <span class="mm-name">${conv.other_user.display_name || conv.other_user.username}</span>
              <span class="mm-time">${time}</span>
            </div>
            <div class="mm-preview">
              <span class="mm-last-msg">${lastMsg}</span>
              ${conv.unread_count > 0 ? `<span class="mm-unread">${conv.unread_count}</span>` : ''}
            </div>
          </div>
        </div>
      `;
    }).join('');

    convList.querySelectorAll('.mm-item').forEach(item => {
      item.addEventListener('click', () => {
        const id = item.getAttribute('data-user-id')!;
        const name = item.getAttribute('data-name')!;
        const avatar = item.getAttribute('data-avatar')!;
        const status = item.getAttribute('data-status')!;
        openChat(id, name, avatar, status);
      });
    });
  }

  function formatTime(dateStr: string): string {
    if (!dateStr) return '';
    const date = new Date(dateStr);
    const now = new Date();
    const diff = now.getTime() - date.getTime();
    const mins = Math.floor(diff / 60000);
    if (mins < 1) return 'Ahora';
    if (mins < 60) return `${mins}m`;
    const hours = Math.floor(diff / 3600000);
    if (hours < 24) return `${hours}h`;
    const days = Math.floor(diff / 86400000);
    if (days < 7) return `${days}d`;
    return date.toLocaleDateString('es', { day: 'numeric', month: 'short' });
  }

  function setupEventListeners() {
    // Tabs
    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        tabs.forEach(t => t.classList.remove('active'));
        tab.classList.add('active');
        currentTab = tab.getAttribute('data-tab') || 'all';
        filterAndRender();
      });
    });

    // Search
    searchBtn.addEventListener('click', () => {
      searchContainer.hidden = !searchContainer.hidden;
      if (!searchContainer.hidden) searchInput.focus();
    });

    searchClear.addEventListener('click', () => {
      searchContainer.hidden = true;
      searchInput.value = '';
      filterAndRender();
    });

    searchInput.addEventListener('input', filterAndRender);

    // Back button
    backBtn.addEventListener('click', closeChat);

    // Message form
    messageForm.addEventListener('submit', sendMessage);
    messageInput.addEventListener('input', () => {
      sendBtn.disabled = messageInput.value.trim().length === 0;
    });

    // New chat modal
    [newChatBtn, startBtn].forEach(btn => {
      btn.addEventListener('click', () => {
        newChatModal.hidden = false;
        loadFriendsForModal();
      });
    });

    modalClose.addEventListener('click', () => {
      newChatModal.hidden = true;
    });

    newChatModal.addEventListener('click', (e) => {
      if (e.target === newChatModal) {
        newChatModal.hidden = true;
      }
    });

    modalSearchInput.addEventListener('input', () => {
      const query = modalSearchInput.value.toLowerCase();
      modalList.querySelectorAll('.mm-modal-item').forEach((item: any) => {
        const name = item.querySelector('.mm-modal-item-name')?.textContent?.toLowerCase() || '';
        const username = item.querySelector('.mm-modal-item-username')?.textContent?.toLowerCase() || '';
        item.style.display = name.includes(query) || username.includes(query) ? 'flex' : 'none';
      });
    });
  }

  async function loadFriendsForModal() {
    if (friends.length === 0) {
      modalList.innerHTML = `
        <div class="mm-modal-loading">
          <span style="color: rgba(255,255,255,0.5);">No tienes amigos aún</span>
        </div>
      `;
      return;
    }

    modalList.innerHTML = friends.map(f => `
      <div class="mm-modal-item" data-id="${f.user_id}" data-name="${f.display_name || f.username}" data-avatar="${f.avatar_url || '/images/default-avatar.svg'}" data-status="${f.status}">
        <img src="${f.avatar_url || '/images/default-avatar.svg'}" alt="" onerror="this.src='/images/default-avatar.svg'" />
        <div class="mm-modal-item-info">
          <div class="mm-modal-item-name">${f.display_name || f.username}</div>
          <div class="mm-modal-item-username">@${f.username}</div>
        </div>
      </div>
    `).join('');

    modalList.querySelectorAll('.mm-modal-item').forEach(item => {
      item.addEventListener('click', () => {
        const id = item.getAttribute('data-id')!;
        const name = item.getAttribute('data-name')!;
        const avatar = item.getAttribute('data-avatar')!;
        const status = item.getAttribute('data-status')!;
        newChatModal.hidden = true;
        openChat(id, name, avatar, status);
      });
    });
  }

  async function openChat(id: string, name: string, avatar: string, status: string) {
    currentChat = { id, name, avatar, status };

    chatName.textContent = name;
    chatAvatar.src = avatar;
    chatProfile.href = `/profile/${id}`;

    chatStatusDot.className = 'mm-chat-status-dot ' + (status === 'online' ? 'online' : status === 'away' ? 'away' : 'offline');
    chatStatus.textContent = status === 'online' ? 'En línea' : status === 'away' ? 'Ausente' : 'Desconectado';
    chatStatus.style.color = status === 'online' ? '#22c55e' : status === 'away' ? '#f59e0b' : 'rgba(255,255,255,0.5)';

    conversationsView.hidden = true;
    chatView.hidden = false;
    messages = [];
    renderMessages();

    await loadConversation(id);
    messageInput.focus();
  }

  function closeChat() {
    chatView.hidden = true;
    conversationsView.hidden = false;

    if (realtimeChannel) {
      supabase.removeChannel(realtimeChannel);
      realtimeChannel = null;
    }
    currentChat = null;
    conversationId = null;
    messages = [];
  }

  async function loadConversation(recipientId: string) {
    if (!userId) return;

    try {
      const { data: existingConvos } = await (supabase as any)
        .from('conversation_participants')
        .select('conversation_id')
        .eq('user_id', userId);

      let convId: string | null = null;

      if (existingConvos && existingConvos.length > 0) {
        const convoIds = existingConvos.map((c: any) => c.conversation_id);
        const { data: sharedConvos } = await (supabase as any)
          .from('conversation_participants')
          .select('conversation_id, conversations!inner(type)')
          .eq('user_id', recipientId)
          .in('conversation_id', convoIds)
          .eq('conversations.type', 'direct');

        if (sharedConvos && sharedConvos.length > 0) {
          convId = sharedConvos[0].conversation_id;
        }
      }

      if (!convId) {
        const { data: newConvo } = await (supabase as any)
          .from('conversations')
          .insert({ type: 'direct' })
          .select('id')
          .single();

        if (newConvo) {
          convId = newConvo.id;
          await (supabase as any)
            .from('conversation_participants')
            .insert([
              { conversation_id: convId, user_id: userId },
              { conversation_id: convId, user_id: recipientId },
            ]);
        }
      }

      conversationId = convId;

      if (convId) {
        await loadMessages(convId);
        subscribeToMessages(convId);
      }
    } catch (e) {
      console.error('Error loading conversation:', e);
    }
  }

  async function loadMessages(convId: string) {
    const { data } = await (supabase as any)
      .from('messages')
      .select('*')
      .eq('conversation_id', convId)
      .order('created_at', { ascending: true })
      .limit(100);

    if (data) {
      messages = data;
      renderMessages();
    }
  }

  function subscribeToMessages(convId: string) {
    if (realtimeChannel) {
      supabase.removeChannel(realtimeChannel);
    }

    realtimeChannel = supabase
      .channel(`messages:${convId}`)
      .on('postgres_changes', {
        event: 'INSERT',
        schema: 'public',
        table: 'messages',
        filter: `conversation_id=eq.${convId}`,
      }, (payload) => {
        const msg = payload.new as Message;
        if (msg.sender_id !== userId) {
          messages.push(msg);
          renderMessages();
        }
      })
      .subscribe();
  }

  function renderMessages() {
    if (messages.length === 0) {
      messagesContainer.innerHTML = `
        <div class="mm-messages-empty">
          <div class="mm-messages-empty-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
            </svg>
          </div>
          <p>No hay mensajes</p>
          <span>¡Envía el primer mensaje!</span>
        </div>
      `;
      return;
    }

    let lastDate = '';
    messagesContainer.innerHTML = messages.map(msg => {
      const msgDate = new Date(msg.created_at).toLocaleDateString('es');
      let dateSep = '';
      if (msgDate !== lastDate) {
        lastDate = msgDate;
        dateSep = `<div class="mm-date-sep"><span>${msgDate}</span></div>`;
      }

      const isMine = msg.sender_id === userId;
      const time = new Date(msg.created_at).toLocaleTimeString('es', { hour: '2-digit', minute: '2-digit' });
      const senderName = currentChat?.name || 'Usuario';
      const senderAvatar = currentChat?.avatar || '/images/default-avatar.svg';

      return `${dateSep}
        <div class="mm-msg ${isMine ? 'sent' : 'received'}">
          ${!isMine ? `<img src="${senderAvatar}" alt="" class="mm-msg-avatar" onerror="this.src='/images/default-avatar.svg'" />` : ''}
          <div class="mm-msg-body">
            ${!isMine ? `<span class="mm-msg-sender">${senderName}</span>` : ''}
            <div class="mm-msg-bubble">
              <p>${escapeHtml(msg.content)}</p>
              <div class="mm-msg-footer">
                <span class="mm-msg-time">${time}</span>
                ${isMine ? `<svg class="mm-msg-check read" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"></polyline></svg>` : ''}
              </div>
            </div>
          </div>
        </div>
      `;
    }).join('');

    messagesContainer.scrollTop = messagesContainer.scrollHeight;
  }

  async function sendMessage(e: Event) {
    e.preventDefault();
    const content = messageInput.value.trim();
    if (!content || !conversationId || !userId) return;

    messageInput.value = '';
    sendBtn.disabled = true;

    const { data: msg, error } = await (supabase as any)
      .from('messages')
      .insert({
        conversation_id: conversationId,
        sender_id: userId,
        content,
      })
      .select()
      .single();

    if (error) {
      console.error('Send error:', error);
      messageInput.value = content;
      return;
    }

    if (msg) {
      messages.push(msg);
      renderMessages();
    }
  }

  function escapeHtml(text: string): string {
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
  }
</script>
